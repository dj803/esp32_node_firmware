# Firmware Memory Budget

Last updated: v0.3.20 (commit 75f14e8, 2026-04-22)

---

## Flash — app partition

| | |
|---|---|
| Firmware binary (`firmware.bin`) | 1.7 MB |
| App partition size (min_spiffs, per OTA slot) | 1.9 MB (1920 KB) |
| **Usage** | **~88%** |
| Headroom | ~200 KB |

OTA requires two equal-sized app partitions (`app0` / `app1`). The firmware
must fit in 1920 KB or the OTA flash step will fail.

### Full partition layout (min_spiffs.csv)

| Partition | Type | Offset | Size |
|---|---|---|---|
| nvs | data | 0x009000 | 20 KB |
| otadata | data | 0x00e000 | 8 KB |
| app0 | app | 0x010000 | **1920 KB** |
| app1 | app | 0x1f0000 | **1920 KB** |
| spiffs | data | 0x3d0000 | 128 KB |
| coredump | data | 0x3f0000 | 64 KB |

---

## IRAM (instruction RAM) — the tight one

| | |
|---|---|
| Physical IRAM (`iram0_0_seg`) | 128 KB (0x20000) |
| v0.3.20 SRAM1 extension (`ld/memory.ld`) | +2 KB (0x800) |
| **Total allocated** | **130 KB** |
| Estimated used | ~130 KB |
| **Usage** | **~98%** |

### Why IRAM is nearly full

IRAM holds functions that must run while the SPI flash cache is flushed or
off — primarily ISR handlers, the Wi-Fi/BLE stack hot paths, FreeRTOS tick,
and (from v0.3.20) the ESP-IDF sleep driver (`esp_sleep.c`). These are
marked `IRAM_ATTR` in the IDF source and cannot be moved to flash.

The 2 KB extension added in v0.3.20 (`ld/memory.ld` + `modify_link_path.py`)
uses SRAM1 dual-port memory at 0x400A0000 — the same physical chip accessible
on both the instruction and data buses. The linker script already handles heap
shrinkage automatically via `_sram1_iram_len`.

### Adding future features that need IRAM

If a future driver overflows IRAM again, bump the extension in `ld/memory.ld`:

```ld
iram0_0_seg (RX) : org = 0x40080000, len = 0x20000 + 0x800
```

Change `0x800` (2 KB) to `0x1000` (4 KB), `0x1800` (6 KB), etc. Each 4 KB
added here costs 4 KB of runtime heap (from SRAM1 dual-port region). The
heap at runtime is typically ~200 KB, so there is room for several more
increments before heap becomes a concern.

Maximum safe SRAM1 extension: ~32 KB (0x8000). Beyond that you risk heap
exhaustion under peak load (BLE scan + RFID + ESP-NOW all active).

---

## DRAM (data RAM — static allocations)

| | |
|---|---|
| `dram0_0_seg` capacity | ~122 KB |
| Static `.data` (init, copied from flash at boot) + `.bss` (zero-init) | see `xtensa-esp-elf-size` below |
| Runtime heap | grows into remaining ~300+ KB of DRAM not used by static allocations |

### Raw `xtensa-esp-elf-size` output (v0.3.20)

```
   text      data       bss       dec
1375136    353781     52956   1781873
```

Note: for ESP32, the `data` column from `xtensa-esp-elf-size` includes
flash-mapped read-only data (`drom0_0_seg`) that does **not** consume RAM at
runtime. The true DRAM static footprint is considerably smaller than 353 KB.
Use `firmware.map` for per-section breakdowns.

---

## Per-component breakdown (v0.3.20)

Generated by `docs/map_parse.py` from `firmware.map`. Ground-truth section
sizes come from the linker map section headers; per-library bytes are
approximate (see note below each table).

### IRAM — top consumers (accurate; no string-merging in IRAM)

| Component | Bytes | KB | % of 132 KB |
|---|---|---|---|
| btdm_app (BLE controller binary) | 32177 | 31.4 | 23.8% |
| freertos (RTOS kernel — task scheduler, queues, semaphores) | 16498 | 16.1 | 12.2% |
| c (newlib libc — memcpy, string, printf hot paths) | 13325 | 13.0 | 9.9% |
| phy (RF PHY driver — must run with flash cache off) | 9126 | 8.9 | 6.8% |
| spi_flash (flash cache, MMU, wear-levelling, OTA write) | 8678 | 8.5 | 6.4% |
| esp_hw_support (RTC clock, CPU freq scaling, peripheral init) | 7966 | 7.8 | 5.9% |
| heap (TLSF heap allocator — ISR-safe alloc/free) | 6428 | 6.3 | 4.8% |
| hal (hardware abstraction — GPIO, timer, SPI, I2C, cache) | 5557 | 5.4 | 4.1% |
| esp_system (panic handler, reset reason, IDF system init) | 4223 | 4.1 | 3.1% |
| esp_ringbuf (IDF ring buffer — used by UART and streams) | 4160 | 4.1 | 3.1% |
| coexist (Wi-Fi + BLE radio coexistence scheduler) | 3256 | 3.2 | 2.4% |
| esp_sleep / rtc (sleep entry, RTC timer — added v0.3.20) | 2028 | 2.0 | 1.5% |
| pp (Wi-Fi MAC lower-layer — packet processing) | 1937 | 1.9 | 1.4% |
| bootloader_support (flash encryption helpers, partition table) | 1839 | 1.8 | 1.4% |
| xtensa (Xtensa arch — setjmp, window overflow, int vectors) | 1817 | 1.8 | 1.3% |
| newlib (C stdlib bridging — stdio, TZ, malloc hooks) | 1761 | 1.7 | 1.3% |
| esp_driver_rmt (RMT peripheral driver — used by FastLED) | 1736 | 1.7 | 1.3% |
| bt (Classic Bluetooth stack) | 1678 | 1.6 | 1.2% |
| (remaining < 1.5 KB each) | 5663 | 5.5 | 4.2% |
| **TOTAL** | **129855** | **126.8** | **96.1%** |

**Actual `.iram0.text` size: 131363 B = 128.3 KB / 132 KB = 97.2%**

The 2 KB extension into SRAM1 (`ld/memory.ld` `0x20000+0x800`) was added in
v0.3.20 to accommodate the `esp_sleep.c` IRAM_ATTR functions pulled in by
`cmd/sleep` and `cmd/deep_sleep`.

### Flash — top consumers (note: totals overcounted ~10–15% due to merged string literals)

| Component | Bytes | KB | Notes |
|---|---|---|---|
| net80211 (Wi-Fi 802.11 stack) | 169334 | 165.4 | Largest single consumer |
| SPI (Arduino SPI + FastLED LED strip driver) | 151185 | 147.6 | Inflated by str1.1 merging |
| lwip (TCP/IP — sockets, DHCP, DNS) | 133124 | 130.0 | |
| mbedcrypto (TLS symmetric + asymmetric crypto) | 119406 | 116.6 | AES, SHA, ECDH |
| NimBLE-Arduino (BLE host — GATT, GAP, connections) | 107162 | 104.7 | |
| src (firmware application code) | 104549 | 102.1 | Our own code |
| btdm_app (BLE controller binary) | 101555 | 99.2 | Closed-source blob |
| c / libc (newlib C standard library) | 67687 | 66.1 | printf, string, math |
| pp (Wi-Fi MAC lower-layer) | 67202 | 65.6 | Rate ctrl, aggregation |
| wpa_supplicant (WPA2/WPA3 auth, 802.11 association) | 61198 | 59.8 | |
| FrameworkArduino (Arduino-ESP32 core) | 54707 | 53.4 | HAL glue, Arduino APIs |
| FastLED (LED animation library) | 50195 | 49.0 | |
| mbedtls_2 (TLS record layer, X.509 cert handling) | 40724 | 39.8 | |
| phy (RF PHY driver + calibration tables) | 34467 | 33.7 | |
| espressif__mdns (mDNS / DNS-SD service discovery) | 30174 | 29.5 | |
| (remaining ~55 libraries) | ~243000 | ~237 | |
| **TOTAL (overcounted)** | **~1713029** | **~1673** | **~87% of 1920 KB** |

**Actual flash section sizes (ground truth from linker headers):**
- `.flash.text`: 1218928 B = 1190.4 KB
- `.flash.rodata`: 325628 B = 318.0 KB
- **Flash total: 1544556 B = 1508.4 KB = 78.6% of 1920 KB partition**

The firmware binary is ~1.7 MB which includes padding and image headers on
top of the section content. The overcounting in the breakdown table comes from
merged string-literal sections (`.rodata.*.str1.1`): the linker merges
duplicate strings across translation units, keeping one physical copy, but the
map file still lists each contributing object at the same address.

### DRAM — top consumers (accurate; no string-merging in DRAM)

| Component | Bytes | KB | % of 122 KB |
|---|---|---|---|
| src (firmware static + BSS variables) | 16147 | 15.8 | 12.9% |
| net80211 (Wi-Fi stack — BSS tables, association state) | 9420 | 9.2 | 7.5% |
| NimBLE-Arduino (BLE host — GATT tables, connection state) | 5734 | 5.6 | 4.6% |
| spi_flash (flash driver state — cache maps, sector tracking) | 5517 | 5.4 | 4.4% |
| freertos (RTOS — task control blocks, timer lists) | 5301 | 5.2 | 4.2% |
| bt (Classic Bluetooth state — HCI, L2CAP buffers) | 4658 | 4.5 | 3.7% |
| lwip (TCP/IP — socket PCBs, ARP table, DHCP lease) | 4397 | 4.3 | 3.5% |
| pp (Wi-Fi MAC — packet queues, rate control state) | 4183 | 4.1 | 3.3% |
| btdm_app (BLE controller — HCI/ACL buffer pools) | 4079 | 4.0 | 3.3% |
| phy (RF calibration data — loaded at boot from NVS) | 3491 | 3.4 | 2.8% |
| FrameworkArduino (Arduino core globals — Serial, WiFi events) | 3083 | 3.0 | 2.5% |
| c / libc (stdio buffers, TZ state, reentrancy structs) | 2530 | 2.5 | 2.0% |
| espressif__mdns (mDNS service registry and query state) | 2414 | 2.4 | 1.9% |
| (remaining ~20 libraries) | 8979 | 8.8 | 7.2% |
| **TOTAL** | **84933** | **82.9** | **68.0%** |

**Actual DRAM static footprint (ground truth):**
- `.dram0.data` (zero-init copy from flash at boot): 28153 B = 27.5 KB
- `.dram0.bss` (zero-init): 52912 B = 51.7 KB
- **DRAM static total: 81065 B = 79.2 KB / 122 KB = 64.8%**

Remaining ~300+ KB of DRAM is available for the runtime heap. The heap at
runtime is typically ~200 KB (measured with `ESP.getFreeHeap()`).

---

## How to re-check after a build

```bash
# From the esp32_node_firmware directory:

# Firmware binary size (compare to 1920 KB app partition limit)
ls -lh .pio/build/esp32dev/firmware.bin

# Section sizes
~/.platformio/packages/toolchain-xtensa-esp-elf/bin/xtensa-esp-elf-size \
    .pio/build/esp32dev/firmware.elf

# Memory region usage (search for iram0_0_seg / dram0_0_seg lines)
grep -E "iram0_0_seg|dram0_0_seg|drom0_0_seg" \
    .pio/build/esp32dev/firmware.map

# Per-component breakdown (regenerates tables above)
python docs/map_parse.py
```
